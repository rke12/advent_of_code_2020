const passwordBreaker = (lowerLimit = 272091, upperLimit = 815432) => {
    let possiblePasswords = [];

    // Step 1: Loop through values between upperLimit and lowerLimit
    let index = lowerLimit;
    while (index <= upperLimit){
        // Step 2

        // Step 3

        index++;
    }

    // Step 2: for each value in loop, figure out if values are descending
    // if we have a match, add to the possiblePasswords array

    // Step 3: for each value in loop, figure out if values hav adjacent pairs
    // if we have a match, add to the possiblePasswords array

    // Return length of possiblePasswords array
    return possiblePasswords.length;
}
module.exports = passwordBreaker;